{% extends "base.html.twig" %}

{% block body %}
    {% if app.user %}
        <div class="row">
            {% include "withMeTemplates/listeAmis.html.twig" %}
            {# ------------------------------- discusion instantanée #}
            <div class="col-md-5">
                <div class="posts">
                    <h1>File d'actualité</h1>
                    {% include "withMeTemplates/postsAmis.html.twig" %}
                </div>
            </div>
            <div class="col-md-4">
                <div class="posts">
                    <h1>Mes derniers posts</h1>
                    {% include "withMeTemplates/postsUser.html.twig" %}
                </div>
            </div>
        </div>
    {% endif %}
{% endblock %}
